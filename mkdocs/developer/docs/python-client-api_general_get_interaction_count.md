# get\_interaction\_count

**get\_interaction\_count(key)**

Get the interaction count given a key

**params**

**key** - key string for interaction count

**return:**

**integer** - interaction count
